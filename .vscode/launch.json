{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Preview Roxo Theme",
            "type": "extensionHost",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "request": "launch"
        }
    ]
}